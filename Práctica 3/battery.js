if (Modernizr.batteryapi) {
    // supported
} else {
    // not-supported
}
if (Modernizr.contextmenu) {
    // supported
} else {
    // not-supported
}

if (Modernizr.cookies) {
    // supported
} else {
    // not-supported
}